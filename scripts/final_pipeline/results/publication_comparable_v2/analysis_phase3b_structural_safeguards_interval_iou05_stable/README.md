# Phase 3B structural safeguards

This directory contains the complete-coordinate sensitivity analysis and paired protein-clustered bootstrap requested in the structural safeguards handoff.

Catalog status: `final_stable`.

The experimental-PDB increment is stage 03 minus stage 02; stage 02 contains only local sequence/annotation geometry. Bootstrap models are held fixed and test proteins—not segments—are resampled. Strain is not conditioned on PDB-coordinate completeness.
